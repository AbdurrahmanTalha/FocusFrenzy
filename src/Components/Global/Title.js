import React from 'react';

const Title = ({ children }) => {
    return (
        <h2 className="text-3xl text-[#EDEDED] pb-[2px] underline underline-offset-[5px] decoration-[#DA0037]">{children}</h2>
    );
};

export default Title;
